{
  "api_base_url": {
    "type": "string",
    "description": "AperturePrism API 地址（不含末尾斜杠）",
    "hint": "例如 http://127.0.0.1:3300",
    "default": ""
  },
  "api_token": {
    "type": "string",
    "description": "API Bearer 令牌（对应 WEBUI_API_TOKEN）",
    "hint": "留空表示 API 未开启鉴权",
    "default": ""
  },
  "default_repo": {
    "type": "string",
    "description": "默认仓库（owner/repo），命令不带仓库时使用",
    "hint": "例如 BB0813/HelixOS",
    "default": ""
  },
  "whitelist": {
    "type": "list",
    "items": { "type": "string" },
    "description": "允许使用的 QQ 用户 ID（sender user_id / openid）白名单；留空表示允许所有人",
    "hint": "逐条添加 QQ 用户 ID",
    "default": []
  },
  "timeout": {
    "type": "int",
    "description": "HTTP 请求超时（秒）",
    "default": 30
  }
}
